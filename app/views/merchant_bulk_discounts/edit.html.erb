<h1>Edit <%=@merchant.name%>'s Discount #<%=@bulk_discount.id%></h1>
<br>
<%if flash[:messages] %>
<section id="errors">
  <% flash[:messages].each do |message| %>
    <h3>Error: <%= message %></h3>
  <%end%>
</section>
<%end%>
<br>
<section id="update_discount">
  <%= form_with url: "/merchants/#{@merchant.id}/bulk_discounts/#{@bulk_discount.id}", method: :patch, local: true do |form| %>
  <%= form.label :percent_discount, "Percent Discount" %>
  <%= form.number_field :percent_discount, :value => @bulk_discount.percent_discount %>
  <%= form.label :percent_discount, "Threshold" %>
  <%= form.number_field :threshold, :value => @bulk_discount.threshold %>
  <%= form.submit%>
<%end%>
</section>
